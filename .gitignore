# Dependencies
node_modules/

# Build output
dist/
*.local

# Environment variables — REAL SECRETS, never commit
.env
.env.local
.env.production
server/.env

# Editor
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS files
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Prisma (server)
server/dev.db
server/dev.db-journal
server/dist/

# Keep the placeholder .env.example — that's the one we commit
# !.env.example  (already committed, just don't delete it)
